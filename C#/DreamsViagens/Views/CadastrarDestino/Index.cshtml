@model IEnumerable<DreamsViagens.Models.CadastrarDestino>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_Destino)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Preco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Casal_Familia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_Destino)
            </td>
            <td>
                R$@Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Casal_Familia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Destino">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Destino">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Destino">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
